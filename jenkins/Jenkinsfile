pipeline {
    agent {
        label 'my-agent'
    }
    
    stages {
        stage('Test') {
            steps {
                echo 'Running tests'
                sh 'mvn --version'
            }
        }
        stage('Deploy'){
            steps {
                echo 'Deploying...'
            }
        }
    }
}
